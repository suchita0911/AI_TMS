# Render Blueprint for AI Powered TMS
# ------------------------------------
# Deploys three services from this repo:
#   1. tms-db   — managed PostgreSQL 16 (auto-seeded by the backend on startup)
#   2. backend  — FastAPI (Docker). Its public host is injected into the frontend
#                 as BACKEND_ORIGIN (see below) so nginx can proxy to it reliably.
#   3. frontend — nginx serving the built React app and proxying /api -> backend's
#                 public https host (same-origin to the browser, so no CORS; and
#                 a request to the public URL wakes a spun-down free backend).
#
# Deploy: push this repo to GitHub, then Render Dashboard -> New -> Blueprint.
# You'll be prompted for the `sync: false` secrets below.

databases:
  - name: tms-db
    databaseName: tms
    user: tms
    plan: free
    postgresMajorVersion: "16"
    region: oregon

services:
  # ------------------------------- Backend ------------------------------- #
  - type: web
    name: backend
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /health
    envVars:
      # Render routes to this port; the Dockerfile binds uvicorn to $PORT.
      - key: PORT
        value: "8000"
      - key: APP_ENV
        value: production
      - key: DEBUG
        value: "false"
      # Database — mapped from the managed Postgres above (internal network).
      - key: POSTGRES_HOST
        fromDatabase:
          name: tms-db
          property: host
      - key: POSTGRES_PORT
        fromDatabase:
          name: tms-db
          property: port
      - key: POSTGRES_USER
        fromDatabase:
          name: tms-db
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: tms-db
          property: password
      - key: POSTGRES_DB
        fromDatabase:
          name: tms-db
          property: database
      # Security — a strong random key is generated once and kept stable.
      - key: SECRET_KEY
        generateValue: true
      # Bootstrap admin (username fixed; password prompted so it isn't in git).
      - key: FIRST_ADMIN_USERNAME
        value: admin
      - key: FIRST_ADMIN_PASSWORD
        sync: false
      # AI — optional. If unset/invalid, AI features degrade to fallbacks.
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: CLAUDE_MODEL
        value: claude-opus-4-8
      # Email links & CORS. Requests proxied same-origin already carry the right
      # Origin, but set these to the frontend URL after the first deploy for
      # correctness (see DEPLOY.md). Prompted so you can paste the real URL.
      - key: FRONTEND_BASE_URL
        sync: false
      - key: BACKEND_CORS_ORIGINS
        sync: false

  # ------------------------------ Frontend ------------------------------- #
  - type: web
    name: frontend
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    envVars:
      # nginx.conf listens on 80; tell Render to route there.
      - key: PORT
        value: "80"
      # Backend's public host (e.g. backend-xxxx.onrender.com), injected by
      # Render. nginx proxies /api to https://$BACKEND_ORIGIN so the upstream
      # always resolves and a spun-down free backend cold-starts on the request
      # (private-network requests do neither on the free plan).
      - key: BACKEND_ORIGIN
        fromService:
          type: web
          name: backend
          property: host
